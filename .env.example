# Environment Variables Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!

# Flask Configuration
SECRET_KEY=your-secret-key-here-generate-with-secrets.token_hex(32)
FLASK_ENV=development

# For production, change FLASK_ENV to:
# FLASK_ENV=production

# Optional: Database URL (for future SQLite migration)
# DATABASE_URL=sqlite:///papers.db

# Optional: Custom upload folder
# UPLOAD_FOLDER=uploads

# Optional: Max file size in bytes (default: 50MB)
# MAX_FILE_SIZE=52428800
